{
  "name": "turbine-brasil-functions",
  "description": "Cloud Functions do Turbine Brasil — preço server-side, notificação WhatsApp e e-mail sem segredo exposto, auditoria de ações administrativas.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.1",
    "nodemailer": "^6.9.15"
  },
  "private": true
}
